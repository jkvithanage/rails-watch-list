<div class="container">
  <div class="h1">My lists</div>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
  <%= link_to "Create a movie list", new_list_path, class: 'btn btn-success' %>
</div>
